# optional, not used by runtime; kept to satisfy structure references
# If you want to enforce schema with pydantic later, add models here.
def validate_config(cfg):  # pragma: no cover
    return cfg
