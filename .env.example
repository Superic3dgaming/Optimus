# =============================================================================
# OPTIMUS CONFIGURATION FILE
# =============================================================================
# Copy this file to .env and modify the values as needed for your setup.

# --- TRADING MODE ---
# Required: Determines how Optimus operates
# Options: BACKTEST (historical data), PAPER (live data, simulated trades), LIVE (real trades)
OPTIMUS_MODE=BACKTEST

# --- STARTING EQUITY ---
# Required for backtesting: Initial capital amount in USD
OPTIMUS_START_EQUITY=10000

# --- DEBUG SETTINGS ---
# Optional: Enable detailed logging and debug output
# Set to 1, true, or yes to enable
# OPTIMUS_DEBUG=1

# --- API CONFIGURATION ---
# Base URL for Delta Exchange API
OPTIMUS_API_BASE=https://api.delta.exchange

# API endpoint paths (uses v3 by default with automatic fallback to v2/legacy)
# Leave empty to use automatic endpoint discovery with fallback
OPTIMUS_API_PATH_CANDLES=/v3/public/candles
OPTIMUS_API_PATH_PRODUCTS=/v3/public/products

# --- API PARAMETER CONFIGURATION ---
# Parameter names used by the API endpoints
OPTIMUS_CANDLES_INTERVAL_PARAM=interval
OPTIMUS_CANDLES_SYMBOL_PARAM=product_id
# Optional: Override default parameter names if API uses different names
# OPTIMUS_CANDLES_START_PARAM=start_time
# OPTIMUS_CANDLES_END_PARAM=end_time

# --- MARKET CONFIGURATION ---
# Underlying asset for perpetual contracts
OPTIMUS_UNDERLYING=ETHUSD

# Root symbol for options (without expiry/strike details)
OPTIMUS_OPTION_ROOT=ETH

# Option type preference: "call", "put", or "both"
OPTIMUS_OPTION_TYPE=both

# --- PRODUCT ID CONFIGURATION ---
# Required if OPTIMUS_CANDLES_SYMBOL_PARAM=product_id
# Product ID for the perpetual contract (find this from the exchange API)
# For ETHUSD on Delta Exchange, this is typically 3136 but verify your setup
OPTIMUS_PERP_PRODUCT_ID=3136

# --- OPTION SELECTION ---
# Enable automatic option selection (recommended for live markets)
OPTIMUS_AUTO_SELECT=1

# Optional: Explicit option overrides (bypasses auto-selection)
# Use EITHER product_id OR symbol, not both
# OPTIMUS_OPTION_PRODUCT_ID=12345
# OPTIMUS_OPTION_SYMBOL=ETH-29AUG24-2500-C

# --- DISCOVERY SETTINGS ---
# Disable automatic API endpoint discovery
OPTIMUS_DISABLE_DISCOVERY=1

# =============================================================================
# CONFIGURATION VALIDATION NOTES
# =============================================================================
# 1. If OPTIMUS_CANDLES_SYMBOL_PARAM=product_id, you MUST set OPTIMUS_PERP_PRODUCT_ID
# 2. If OPTIMUS_AUTO_SELECT=0, you SHOULD set either OPTIMUS_OPTION_PRODUCT_ID or OPTIMUS_OPTION_SYMBOL
# 3. Product IDs can be found using the /products endpoint of the Delta Exchange API
# 4. All time parameters should be in ISO 8601 format (YYYY-MM-DDTHH:MM:SSZ)
# 5. Check Delta Exchange API documentation for the latest endpoint specifications

# =============================================================================
# TROUBLESHOOTING
# =============================================================================
# If you encounter issues:
# 1. Set OPTIMUS_DEBUG=1 to see detailed API requests
# 2. Check the logs/ directory for error details
# 3. Verify your internet connection and API endpoints
# 4. Ensure product IDs are valid and actively traded
# 5. Check Delta Exchange API status and documentation

