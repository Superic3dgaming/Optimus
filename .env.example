# --- Mode ---
OPTIMUS_MODE=BACKTEST

# --- Autonomous selection enabled by default ---
OPTIMUS_AUTO_SELECT=1          # <-- bot will choose nearest-expiry ATM option automatically

# --- Disable discovery of endpoints (we set them here explicitly) ---
OPTIMUS_DISABLE_DISCOVERY=1
OPTIMUS_API_BASE=https://api.delta.exchange
OPTIMUS_API_PATH_CANDLES=/v3/public/candles
OPTIMUS_API_PATH_PRODUCTS=/v3/public/products

# --- Candles query param names ---
OPTIMUS_CANDLES_INTERVAL_PARAM=interval
# Choose which identity you pass to candles: "product_id" (recommended) or "symbol"
OPTIMUS_CANDLES_SYMBOL_PARAM=product_id
# If your API needs different time param names:
# OPTIMUS_CANDLES_START_PARAM=start_time
# OPTIMUS_CANDLES_END_PARAM=end_time

# --- Market roots ---
OPTIMUS_UNDERLYING=ETHUSD
OPTIMUS_OPTION_ROOT=ETH
# Prefer type: "call", "put", or "both"
OPTIMUS_OPTION_TYPE=both

# --- Perp product id (only required if your candles endpoint expects product_id) ---
# If OPTIMUS_CANDLES_SYMBOL_PARAM=product_id, you MUST set this:
# (ETHUSD perp is often 3136 but verify on your side.)
OPTIMUS_PERP_PRODUCT_ID=3136

# --- Explicit overrides (if you want to bypass auto-select) ---
# EITHER a product id:
# OPTIMUS_OPTION_PRODUCT_ID=12345
# OR a symbol (only if your endpoint supports symbol=):
# OPTIMUS_OPTION_SYMBOL=ETH-29AUG24-2500-C

# Debug prints for requests/params
# OPTIMUS_DEBUG=1

