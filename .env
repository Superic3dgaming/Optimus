# --- Mode ---
OPTIMUS_MODE=BACKTEST

# --- API Configuration ---
# Primary API base URL for Delta Exchange
OPTIMUS_API_BASE=https://api.delta.exchange

# Multiple API bases for fallback (comma-separated)
# OPTIMUS_API_BASES=https://api.delta.exchange,https://api-delta.example.com

# API endpoint paths (try multiple versions for compatibility)
OPTIMUS_API_PATH_CANDLES=/v3/public/candles
OPTIMUS_API_PATH_PRODUCTS=/v3/public/products

# --- Endpoint Discovery ---
# Set to 0 to enable discovery of working endpoints (recommended)
OPTIMUS_DISABLE_DISCOVERY=0

# --- Option Selection Strategy ---
# Auto-select nearest-expiry ATM option (recommended for beginners)
OPTIMUS_AUTO_SELECT=1

# --- Candles Query Parameters ---
OPTIMUS_CANDLES_INTERVAL_PARAM=interval
# Symbol parameter: "product_id" (recommended) or "symbol"
OPTIMUS_CANDLES_SYMBOL_PARAM=product_id
# Time parameter names (usually don't need to change)
# OPTIMUS_CANDLES_START_PARAM=start_time
# OPTIMUS_CANDLES_END_PARAM=end_time

# --- Market Configuration ---
OPTIMUS_UNDERLYING=ETHUSD
OPTIMUS_OPTION_ROOT=ETH
# Option type preference: "call", "put", or "both"
OPTIMUS_OPTION_TYPE=both

# --- Required for product_id mode ---
# Perpetual product ID (required if OPTIMUS_CANDLES_SYMBOL_PARAM=product_id)
# Check Delta Exchange API for current ETHUSD perpetual product ID
OPTIMUS_PERP_PRODUCT_ID=3136

# --- Explicit overrides (bypass auto-selection) ---
# Use either OPTIMUS_OPTION_PRODUCT_ID or OPTIMUS_OPTION_SYMBOL, not both

# Option product ID (for product_id mode) - using a test value
OPTIMUS_OPTION_PRODUCT_ID=12345

# Option symbol (for symbol mode)
# OPTIMUS_OPTION_SYMBOL=ETH-29AUG24-2500-C

# --- Debug and Backtest Settings ---
# Enable verbose logging for troubleshooting
# OPTIMUS_DEBUG=1

# Starting equity for backtesting
# OPTIMUS_START_EQUITY=10000

# --- Notes ---
# 1. If auto-selection fails, set explicit OPTIMUS_OPTION_PRODUCT_ID or OPTIMUS_OPTION_SYMBOL
# 2. Use OPTIMUS_DEBUG=1 to see detailed API requests and responses
# 3. Check Delta Exchange API documentation for current product IDs and endpoints
# 4. Network connectivity issues may require setting working OPTIMUS_API_BASES

